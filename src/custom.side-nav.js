(function($) {
	
	//console.log("Loaded : custom.side-nav.js");

	// attaching event to load results screen to chart icon in side nav
	/* couldn't get the selector figured out so put the myResults function directly on the html tag 
	$('.side-nav-item-8').hammer().on('tap', function(){
		console.log("Side nav : Results icon");
		$('.inputsTray').addClass("fullSize");
		myResults(myParams);
		
        // need to also set on state for nav buttons
	});
	*/

	
	

})(jQuery);
